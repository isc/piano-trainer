// Test stub for @tonejs/piano, which is a browser-only dependency resolved via
// the page's import map at runtime. playback.js imports `Piano` at module load;
// buildCursorTimeline (the unit under test) never instantiates it.
export class Piano {}
